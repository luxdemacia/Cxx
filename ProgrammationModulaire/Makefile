# Fichiers sources
SRCS = App.cxx Message.cxx

# Nom de l'exécutable
TARGET = Message:

# Commande de compilation
all:
	g++ $(SRCS) -o $(TARGET)
# Nettoyage des fichiers temporaires
clean:
	rm -f $(TARGET)
# Exécutable
run:
	./$(TARGET)
